<% title "My Account" %>

<%= render :partial => 'form' %>

<% content_for :sidebar do %>
  <div class="store-balance">
    Your Balance: <%= number_to_currency @user.store.balance %>
  </div>
  <% if !@user.api_key.blank? %>
    <ul>
      <li class="sidebar">
        <p>
          <strong>Your API Key: </strong>
          <small>
            <%= icon "key" %> <%= @user.api_key %>
          </small>
        </p>
      </li>
    </ul>
  <% end %>
  <h2>Recent Deliveries</h2>
  <ul>
    <% @user.store.deliveries.delivered(:limit => 10).reverse.each do |delivery| %>
      <li><%= link_to image_tag("icons/pdf.gif", :style => "vertical-align: top;"), @current_user.super? ? delivery_path(delivery, :format => :pdf) : store_delivery_path(@store, delivery, :format => :pdf) %> <%= link_to delivery.delivered_at.strftime("%m / %d / %Y"), @current_user.super? ? delivery : store_delivery_path(@store, delivery) %> <%= number_to_currency(delivery.total) %></li>
    <% end %>
  </ul>
<% end %>
